edge_lists/ is a directory containing the edge lists used for these analyses in CSV (comma-separated values) format, and contains a readme with more information.

expression_data/ is a directory containing the expression data used for these analyses, in TSV (tab-separated values) format, and contains a readme with more information..

Both formats are readily consumed by Microsoft Excel, LibreOffice Calc, Google Sheets and the majority of programming languages.

